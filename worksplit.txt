2 Person, 2 Day Plan

Day 1 — June 16 (Today)
Person A (You)

Install Erlang/OTP
Learn: spawn, send/receive, nodes — just 1 hour of basics
Build: Erlang WebSocket server using cowboy library
Goal by night: Erlang server running, accepting connections on localhost

Person B (Friend)

Embed Monaco Editor in plain HTML via CDN
Learn Yjs basics (30 min docs)
Connect Yjs to two browser tabs, prove sync works locally
Goal by night: two tabs editing same document live

End of Day 1 sync:

Both sit together, connect Person B's browser to Person A's Erlang server via WebSocket. One machine, both components talking.

Day 2 — June 17 (Tomorrow)
Person A

Add Firebase room code system (create room → get 6 digit code → share)
Handle peer discovery logic in Erlang
Goal: two different laptops finding each other via room code

Person B

Polish Monaco UI (syntax highlighting, language selector, dark theme)
Write one Lua plugin (fake it if needed — just show a script running)
Add basic UI: room code input box, connection status, user indicator

End of Day 2:

Full demo run — A and B on different machines, enter room code, edit together live. Record it immediately.

Split Logic
TaskWhoErlang backendYouMonaco + Yjs frontendFriendFirebase integrationYouUI polishFriendLua pluginEitherDemo recordingBoth

Tools to install RIGHT NOW
You:
bash# Install Erlang
# Windows: download from erlang.org installer
# Then install rebar3 (Erlang build tool)
Friend:
html<!-- Monaco CDN test — paste in browser -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.44.0/min/vs/loader.min.js"></script>
